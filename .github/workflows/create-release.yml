name: Create Release

on:
  pull_request:
    types:
      - closed
  workflow_dispatch:
    inputs:
      version:
        description: "Version to release (e.g., 1.3.0)"
        required: true
        type: string

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    if: "${{ github.event_name == 'workflow_dispatch' || (github.event.pull_request.merged == true && contains(github.event.pull_request.labels.*.name, 'autorelease: pending')) }}"
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.merge_commit_sha || github.sha }}

      - uses: georgeguimaraes/workflows/actions/extract-version@main
        id: version
        with:
          version: ${{ inputs.version }}
          pr-title: ${{ github.event.pull_request.title }}

      - name: Create tag
        env:
          TAG: v${{ steps.version.outputs.version }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git tag "$TAG"
          git push origin "$TAG"
        # The tag push will trigger the CI workflow which builds binaries and publishes

      - uses: georgeguimaraes/workflows/actions/mark-release-tagged@main
        with:
          pr-number: ${{ github.event.pull_request.number }}
